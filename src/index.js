import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Add the entire application to the element with the ID of 'root' in the 'public/index.html' file
ReactDOM.render(<App/>, document.getElementById('root'));